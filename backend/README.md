# Backend — E-Ticket Internal System

NestJS + Prisma (MySQL). Dokumentasi setup lengkap ada di README root repo.

## Scripts
- `npm run start:dev` — dev server
- `npm run build` — compile
- `npm test` — unit test
- `npm run seed` — seed user dari prisma/users.csv